# Terminal-based Interface Instructions

## Building

### Dependencies:

- cmake
- gcc
- glibc
- Python 3.7 (plotter)
- matplotlib (plotter)
- numpy (plotter)

1. Go to `<project-root>/controlsoftware/raw_serial`
1. create a new directory named `build`
1. run `cmake ..`
1. run `make`

## Instructions

### `read` Binary

1. plug in USB rs485 connector
1. run `ls /dev` and ensure the `ttyUSB0` device is available
1. run the `read` binary with root permissions
1. when done, press ctrl+c to exit program

### Log Plotting

The python plotting program can be invoked as such:

`plot.py /path/to/log`

The log is log.txt by default.
